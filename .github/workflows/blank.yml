name: CI
on:
  push:
  pull_request:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: fail
        run: "false"
      - name: oke
        run: "true"
      - name: check
        if: ${{ failure() }}
        run: echo hello
